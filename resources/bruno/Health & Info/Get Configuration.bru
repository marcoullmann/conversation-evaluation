meta {
  name: Get Configuration
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/config
  body: none
  auth: none
}

tests {
  test("Status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Response contains config", function() {
    const body = res.getBody();
    expect(body.bigquery).to.not.be.undefined;
    expect(body.evaluation).to.not.be.undefined;
    expect(body.llm).to.not.be.undefined;
  });
}
