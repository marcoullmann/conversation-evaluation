meta {
  name: Stop Evaluation
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/evaluation/{{job_id}}/stop
  body: none
  auth: none
}

tests {
  test("Status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Response contains success message", function() {
    const body = res.getBody();
    expect(body.message).to.not.be.undefined;
    expect(body.message).to.include('stopped successfully');
  });
}
