meta {
  name: List Evaluations (Filtered by Date)
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/evaluations?start=2024-01-01
  body: none
  auth: none
}

tests {
  test("Status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Response contains evaluations list", function() {
    const body = res.getBody();
    expect(Array.isArray(body.evaluations)).to.be.true;
  });
}
